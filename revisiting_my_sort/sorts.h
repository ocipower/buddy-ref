#ifndef SORT_H
#define SORT_H

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>
void bubble_sort(int *array, size_t size);
void swap(int *a, int *b);
void print_array(int array[], int len);

#endif